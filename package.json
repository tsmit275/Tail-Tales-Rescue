{
  "name": "tail-tales-rescue",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build",
    "start:dev": "concurrently \"cd backend && node server.js\" \"cd frontend && npm start\"",
    "install": "cd backend && npm i && cd .. && cd frontend && npm i",
    "postinstall": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
